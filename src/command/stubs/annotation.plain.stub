<?php
declare (strict_types = 1);

namespace {%namespace%};

use Doctrine\Common\Annotations\Annotation;

/**
 * class {%className%}
 * @package {%namespace%}
 * @Annotation
 * @Target({"METHOD","CLASS"}) # 不需要进行类注解去掉"CLASS"，不需要方法注解去掉"METHOD"
 */
class {%className%} extends Annotation
{
    // TODO 完成你对注解字段的定义
}
